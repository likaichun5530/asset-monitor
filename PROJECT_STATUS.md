# 项目现状与审查记录

更新日期：2026-09-20。最近一次完整项目审查覆盖前后端引用、依赖、静态资源、原生工程遗留、构建与部署配置、自动化测试和项目文档；本次桌面 UI 改版未访问实盘表格或执行资产数据写入。

## 版本与环境

| 项目 | 当前状态 |
| --- | --- |
| 应用 | 有数 / Asset Monitor |
| 生产地址 | https://asset.kenny5530.asia |
| Git | https://github.com/likaichun5530/asset-monitor.git ，main |
| 本地版本 | 2.5.32，已部署；资产趋势嵌入总资产卡并修正桌面字体字面 |
| 最近已确认生产版本 | 2.5.32 |
| 最近已确认部署 | dpl_ANJfGPrV9iRbhXcMgU9qZ6bCje3M，READY，已绑定生产域名 |
| 最近已确认 Git 提交 | 本文件所在的 2.5.28 发布提交；前一版本 99289d8 |
| 用户使用方式 | Android Chrome 添加到主屏幕的 PWA |
| Android 原生 | Capacitor 8；本地 versionName 2.5.32 / versionCode 150；本批次未构建 APK |
| 本地 API 包 | server/package.json 的 2.1.10 是开发容器的独立版本 |

## 业务规则

- 评分为 5～100 的整数，不包含每日涨跌与目标合理性评价；保留从 100 开始的动画。
- 容忍偏差为目标的 40%，最低 0.5、最高 2 个百分点；只对超出容忍范围的部分扣分，边界本身不触发提醒。
- 大类每超出 1 个百分点扣 2 分，合计封顶 50；细分每超出 1 个百分点扣 1.5 × 大类权重，权重为实际占比和目标占比较大值，合计封顶 30。
- 现金、期货、黄金无需细分目标；未设置目标的“其他”不参与细分评分。
- IC 保证金比例 ≤70% 不扣分；70%～75% 每个百分点扣 1 分，75%～80% 扣 2 分，80% 以上扣 3 分，分段累计封顶 60。按 14% 保证金率估算，用户已确认账户权益分母与该比例适用。
- 持仓与历史按页面依赖每 60 秒刷新；Market / Futures 独立轮询仍为 5 分钟。
- PWA 不主动指定系统状态栏颜色；页面主题独立切换。暗夜卡片色为 #1f2937，页面底色 #111827。

## 2.5.28 清理结果

- 删除无构建入口的 Electron 主进程、过时的 target 表初始化脚本、无引用的 StatCard、未使用的 origin.png 和两个 Android 模板测试。保留当前平台代码、品牌图与对话框仍使用的助手头像。
- 移除旧脚本专用 googleapis 依赖及 26 个安装包；本地 API 继续复用现有零 Google SDK 依赖的生产处理器。
- 移除无调用的前端快照创建链、币种汇总函数、重复 AI 规则别名、旧目标序列化函数、未使用的参数 / 导入 / 样式，以及现金建议中无人使用的中间结果。保留服务端快照接口、Cron 和旧待同步队列的合并与重试。
- README 改正首页、趋势范围、持仓概况、AI 入口、快照入口、表结构及开发配置；打包指南删除不再可用的 Electron 流程与历史本机产物说明。前端环境示例仅保留公开配置。
- 补全已有交互和暗夜样式引用但未定义的 brand-200 / 300 / 400 色阶；不更改 AI 黑白样式。
- 修正 5 个历史测试失败：旧字号、旧刷新周期与评分边界 / 说明断言；评分规则实现未变动。

## 2.5.29 桌面 UI

- 桌面侧栏改为深墨色导航底座，重新整理品牌区、导航分组、图标容器和选中状态；移动端顶部栏和底部导航不变。
- 桌面顶栏压缩为 76px，与最大 1480px 的内容列对齐，统一在线状态、刷新、用户和设置控件的尺寸与圆角。
- 首页总资产改为唯一的深色主视觉面板，今日盈亏独立分区；普通卡片使用低阴影、细边框和 14px 圆角，强化信息层级并保持暗夜模式一致。
- 已用隔离 Chrome 演示数据检查 1440px 总览、持仓和暗夜总览的实际渲染；移动端继续使用原有规则。

## 仍需单独处理的问题

1. **共享期货账户计算。** 首页取单条 IC 使用率最大值，期货页逐条汇总市值。多份合约共用账户且重复记录权益时，可能低估保证金使用率。本次不擅自更改账户权益模型；后续需按账户合计合约保证金、去重权益，并配合具体数据验证。
2. **一分钟刷新未完全统一。** Market / Futures 的独立轮询仍为 5 分钟，应与用户希望的一分钟统一；本次保持刷新行为，文档已如实说明。
3. **打新提醒失败可能闪动。** 同日请求失败也会清空提醒；后续应保留同日有效结果，仅在跨日时清空。
4. **主 JS 包仍偏大。** 当前约 872 kB（gzip 253 kB），主要包含图表与页面。删除未引用源码不会显著减少已被构建器剔除的代码；后续可单独做路由按需加载，并验证页面切换不会引入闪动。
5. **真机验证。** Android / iPhone 状态栏、软键盘及 APK 构建不在本次验证范围，不能以 Web 构建通过替代。

## 验证

- 全量 Node 测试 134 项通过；其中部分是源码回归检查，不能替代完整端到端测试。
- 版本一致性、生产构建通过；PWA 预缓存由 3282.65 KiB 降至约 2907 KiB，减少约 375 KiB（11.4%）。
- 122 个 JS/JSX 模块导入与 README / 打包 / 状态文档链接检查通过。
- 隔离 Chrome 演示模式在 390px 和 1280px 宽度下各验证 16 个页面均渲染，无未捕获浏览器异常；未验证真实联网业务操作。
- 本地 API 在无实盘凭据的独立端口启动成功；健康检查及缺少 JWT 配置时拒绝私人请求通过。
- 未执行实盘持仓 / 目标 / 密码写入、真实 AI 调用、APK 打包或手机真机测试。
- Vercel 生产部署返回 READY，已绑定正式域名；源码和发布记录随本批次提交。
